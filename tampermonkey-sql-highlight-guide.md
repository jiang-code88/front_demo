# SQL Editor (CodeMirror 6) — 产品功能介绍与使用指南

> Tampermonkey 油猴脚本 · v2.2.0
> 为 SQL 工具页面（sqltools）提供现代化 SQL 编辑器：语法高亮、自动补全、语法诊断、多 Tab 独立查询。

---

## 一、产品简介

本脚本会将运维平台 SQL 查询页面的原生文本框，替换为一个基于 **CodeMirror 6** 的专业 SQL 编辑器，并将原本"提交即整页刷新"的查询流程改造为 **AJAX 局部刷新**，同时引入 **多 Tab 独立查询** 能力。

**一句话总结：把简陋的 SQL 查询页，升级为接近本地 IDE 的查询工作台。**

### 适用站点

- `vinops.qipeipu.net/operate/sqltools*`
- `ops.qipeipu.net/operate/sqltools*`

---

## 二、核心功能

### 1. 现代 SQL 编辑器

| 能力 | 说明 |
|------|------|
| 语法高亮 | MySQL 方言，关键字自动大写显示 |
| 自动补全 | 标准 MySQL 关键字 |
| 语法诊断 | 真实 MySQL 解析器校验，错误处显示波浪线，行号栏显示错误图标（悬浮查看详情） |
| 编辑体验 | 行号、括号匹配、代码折叠、当前行高亮、自动换行、撤销/重做 |
| 主题 | One Dark 暗色主题，等宽字体 |

### 2. 多 Tab 独立查询

- **每个 Tab 拥有独立的内容、数据库选择、撤销栈、查询结果**
- 新建 / 切换 / 关闭 / 重命名（双击 Tab 名）Tab
- Tab 状态（SQL 文本、所选数据库、Tab 名称）**自动持久化**，刷新页面后自动恢复
- 不同域名（vinops / ops）的 Tab 数据相互隔离，互不干扰

### 3. AJAX 查询与结果面板

- 提交查询**不再整页刷新**，结果直接渲染在页面下方结果面板
- **查询结果按 Tab 保存**，切换 Tab 时结果跟着切换
- 集成分页操作：上一页 / 下一页 / 跳转指定页
- 长查询等待期间，提交按钮变身为 **"取消查询"**，可随时中止

---

## 三、安装

1. 浏览器安装 [Tampermonkey](https://www.tampermonkey.net/) 扩展
2. Tampermonkey 面板 → 添加新脚本 → 粘贴 `tampermonkey-sql-highlight.user.js` 全部内容并保存
3. 打开 SQL 工具页面（vinops / ops 域名均可），编辑器自动生效

> 首次加载需从 esm.sh 拉取 CodeMirror 6 模块，可能响应会比较慢。

---

## 四、使用指南

### 基本查询流程

1. 打开 SQL 工具页面，在编辑器中输入 SQL（可享受补全与语法检查）
2. 在页面上方下拉框**选择要查询的数据库**
3. 点击 **提交** 按钮或按 `Ctrl+Enter`（Mac: `Cmd+Enter`）
4. 查询结果显示在编辑器下方的结果面板中
5. 需要分页时，直接在结果面板下方操作上一页 / 下一页 / 输入页码回车

### 多 Tab 操作

| 操作 | 方式 |
|------|------|
| 新建 Tab | 点击 Tab 栏右侧 **+** 按钮 |
| 切换 Tab | 点击目标 Tab 标签 |
| 重命名 Tab | **双击** Tab 名称，输入后回车（Esc 取消） |
| 关闭 Tab | 点击 Tab 标签上的 **×**（至少保留 1 个） |
| 查看某 Tab 的结果 | 切换到该 Tab，结果面板自动跟随 |

> 每个 Tab 记住自己的 SQL、数据库选择与查询结果；`Ctrl+Z` 撤销历史也是各 Tab 独立的。

### 取消长查询

查询进行中，提交按钮变为橙色 **"取消查询"**，点击即可中止等待（此时按 `Ctrl+Enter` 同样是取消）。取消后可直接修改 SQL 重新提交。

### 油猴菜单命令

点击浏览器 Tampermonkey 图标，可见两个菜单：

| 菜单 | 作用 |
|------|------|
| SQL 高亮：启用/禁用 | 全局开关，切换后自动刷新页面生效 |
| SQL 高亮：清除已保存的 Tab 数据 | 清空所有已持久化的 Tab 状态，恢复初始单 Tab |

### 数据持久化说明

- **会保存**：SQL 文本、Tab 名称、每个 Tab 所选的数据库/ERP 子库、当前激活的 Tab
- **不保存**：查询结果与撤销历史——刷新后结果面板为空，需重新提交查询（避免展示过期数据）
- 打字过程中的保存有 400ms 防抖，刷新/关闭页面前会进行一次数据持久化保存。
